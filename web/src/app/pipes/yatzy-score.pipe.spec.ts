import { YatzyScorePipe } from './yatzy-score.pipe';

describe('YatzyScorePipe', () => {
  it('create an instance', () => {
    const pipe = new YatzyScorePipe();
    expect(pipe).toBeTruthy();
  });
});
